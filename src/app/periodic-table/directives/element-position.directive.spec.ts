import { ElementPositionDirective } from './element-position.directive';

describe('ElementPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementPositionDirective();
    expect(directive).toBeTruthy();
  });
});
