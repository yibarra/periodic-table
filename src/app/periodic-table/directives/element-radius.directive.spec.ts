import { ElementRadiusDirective } from './element-radius.directive';

describe('ElementRadiusDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementRadiusDirective();
    expect(directive).toBeTruthy();
  });
});
